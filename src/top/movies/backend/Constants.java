package top.movies.backend;

public final class Constants {
	// < -------------------------------------------------- >
	// constants class so that certain parameters stay consistent over the whole program without fail
	// can edit here, keep consistency, and easily test the program with different constants
	
	// < -------------------------------------------------- >
	public static final int LOADINGDELAY = 3500; // simulated time for data about a movie to load (in miliseconds)
	public static final int YVELOCITY = 2; // image Y-value will increment by 2 on each repaint() call
	public static final int MAXMOVIECOUNT = 99; // user cannot store more than 99 movies in the list
	
}
